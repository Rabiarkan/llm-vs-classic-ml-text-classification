{
  "name": "genai-m2",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "rm -f .git/hooks/pre-push && pip install --upgrade pip && pip install -r requirements.txt && python -m ipykernel install --user --name genai --display-name 'Python (genai)'",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"]
    }
  },
  "remoteEnv": {
    "PYTHONPATH": "${containerWorkspaceFolder}"
  }
}